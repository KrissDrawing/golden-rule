const initialState = {
  task: ["dupa", "wolololo"],
};

const rootReducer = (state = initialState, action) => {
  return state;
  //   switch (action.type) {
  //     case "REMOVE_ITEM":
  //       return {
  //         ...state,
  //         task:
  //       };
  //   }
};

export default rootReducer;
